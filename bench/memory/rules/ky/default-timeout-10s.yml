id: default-timeout-10s
type: decision
symbol: timeout
canonical: "Default request timeout should be 10 seconds."
rejected: "no default timeout"
paraphrases:
  - "let's default the timeout to 10 seconds"
  - "requests should time out after 10s by default"
  - "set a sensible 10 second default timeout"
test_task: "Decide on a default request timeout and outline the implementation."
adherence_check:
  kind: judge
  expect: "Proposes a 10 second (10000ms) default timeout; does not leave requests without a default timeout."
tension: "Requires choosing 10s over leaving the timeout unbounded."
surface_markers: ["timeout", "10"]
