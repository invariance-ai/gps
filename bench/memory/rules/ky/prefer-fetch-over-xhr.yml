id: prefer-fetch-over-xhr
type: preference
canonical: "Build on the native fetch API, not XMLHttpRequest."
paraphrases:
  - "stick with fetch, don't reach for XMLHttpRequest"
  - "we standardize on the fetch API rather than XHR"
  - "prefer native fetch over XMLHttpRequest everywhere"
test_task: "Propose the underlying HTTP transport for the client and justify the choice."
adherence_check:
  kind: judge
  expect: "Chooses the native fetch API as the transport; does not adopt XMLHttpRequest."
tension: "Requires explicitly choosing fetch over XHR when both are plausible."
surface_markers: ["fetch"]
