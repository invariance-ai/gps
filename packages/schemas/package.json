{
  "name": "@invariance/gps-schemas",
  "version": "0.2.1",
  "description": "Shared types and schemas for gps. Source of truth for CLI, MCP, and HTTP surfaces.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/invariance-ai/gps.git",
    "directory": "packages/schemas"
  },
  "homepage": "https://github.com/invariance-ai/gps#readme",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "json",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "rm -rf dist && tsc"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
