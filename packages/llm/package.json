{
  "name": "@invariance/gps-llm",
  "version": "0.1.0",
  "description": "Thin Anthropic SDK wrapper for gps's LLM-assisted commands (postmortem, attach, pr-intent).",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/invariance-ai/gps.git",
    "directory": "packages/llm"
  },
  "homepage": "https://github.com/invariance-ai/gps#readme",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "rm -rf dist && tsc"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "@invariance/gps-schemas": "workspace:*",
    "yaml": "^2.5.0"
  }
}
