{
  "name": "@invariance/gps-core",
  "version": "0.2.1",
  "description": "Parser, resolver, graph store, and query layer for gps.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/invariance-ai/gps.git",
    "directory": "packages/core"
  },
  "homepage": "https://github.com/invariance-ai/gps#readme",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist/**",
    "grammars/**/*.wasm",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "rm -rf dist && tsc"
  },
  "dependencies": {
    "@invariance/gps-schemas": "workspace:*",
    "chokidar": "^4.0.0",
    "fast-glob": "^3.3.2",
    "typescript": "^5.6.0",
    "web-tree-sitter": "^0.26.8",
    "yaml": "^2.5.0"
  }
}
