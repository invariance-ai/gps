{
  "name": "@invariance/gps-mcp",
  "version": "0.3.0",
  "description": "MCP server for gps — exposes get_context/impact_of/tests_for/invariants_for/prepare_edit to coding agents.",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/invariance-ai/gps.git",
    "directory": "packages/mcp"
  },
  "homepage": "https://github.com/invariance-ai/gps#readme",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "rm -rf dist && tsc"
  },
  "dependencies": {
    "@invariance/gps-core": "workspace:*",
    "@invariance/gps-llm": "workspace:*",
    "@invariance/gps-schemas": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
